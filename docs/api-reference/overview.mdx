---
title: "API Reference"
sidebarTitle: "Overview"
description: "Complete reference for all OpenProjectIdea API endpoints."
---

All endpoints require a valid API key via the `x-api-key` header. Every response is JSON.

## Response Structure

```json
{
  "data": "...",   // The response payload (object or array)
  "meta": { ... }  // Pagination info, mode, etc. (when applicable)
}
```

<AccordionGroup>
  <Accordion title="Response conventions" icon="file-json" defaultOpen>
    - **Lists** return `{ data: [], meta: { total, limit, offset } }`
    - **Single items** return `{ data: { ... } }`
    - **Write operations** return `{ data: { ... } }` with status `201`
    - **Deletes** return `{ deleted: true }`
    - **Errors** return `{ error: "code", message: "..." }` with HTTP status
  </Accordion>
</AccordionGroup>

## Authentication

Include your API key in every request:

```bash
curl -H "x-api-key: osk_your_key_here" \
  https://openprojectidea.com/v1/api/ideas
```

<Tip>
Generate your first key from the [Developer Dashboard](https://openprojectidea.com/developer-api) and get **$5.00 free credit** instantly.
</Tip>

## Pagination

List endpoints support `limit` and `offset` query parameters:

| Parameter | Default | Max | Description |
|---|---|---|---|
| `limit` | 20 | 50 | Number of items to return |
| `offset` | 0 | - | Number of items to skip |

## HTTP Status Codes

| Code | Meaning |
|---|---|
| `200` | Success |
| `201` | Created (POST) |
| `400` | Bad request (missing/invalid params) |
| `401` | Invalid or missing API key |
| `402` | Insufficient credits |
| `403` | Revoked key or insufficient scope |
| `404` | Resource not found |
| `409` | Conflict (e.g. duplicate bookmark) |
| `500` | Internal server error |

---

<CardGroup cols={2}>
  <Card title="Ideas API" icon="lightbulb" href="/api-reference/ideas/list-ideas">
    Browse, search, and get full details for AI-generated ideas.
  </Card>
  <Card title="Comments API" icon="message-circle" href="/api-reference/comments/list-comments">
    Read and post comments on ideas.
  </Card>
  <Card title="Votes API" icon="arrow-up-circle" href="/api-reference/votes/get-votes">
    Cast upvotes/downvotes and check vote status.
  </Card>
  <Card title="Account API" icon="user" href="/api-reference/account/get-credits">
    Check credits, profile, and usage history.
  </Card>
</CardGroup>
