---
title: "Credits & Billing"
sidebarTitle: "Credits & Billing"
description: "Pay-per-use pricing with transparent costs per API call."
---

API access uses a credit-based system. Each call deducts a small amount from your balance. Many endpoints are completely free.

<CardGroup cols={3}>
  <Card title="$0.01" icon="circle-dot">
    Most read endpoints
  </Card>
  <Card title="$0.02" icon="search">
    Search & write endpoints
  </Card>
  <Card title="Free" icon="check">
    Categories, stats, account, deletes
  </Card>
</CardGroup>

---

## Pricing Table

<AccordionGroup>
  <Accordion title="Paid endpoints" icon="wallet" defaultOpen>
    | Endpoint | Cost |
    |---|---|
    | `GET /ideas` | $0.01 |
    | `GET /ideas/search` | $0.02 |
    | `GET /ideas/trending` | $0.01 |
    | `GET /ideas/:slug` | $0.01 |
    | `GET /ideas/:id/comments` | $0.01 |
    | `POST /ideas/:id/comments` | $0.02 |
    | `POST /ideas/:id/vote` | $0.01 |
    | `GET /bookmarks` | $0.01 |
    | `POST /bookmarks` | $0.01 |
  </Accordion>

  <Accordion title="Free endpoints" icon="badge-check">
    | Endpoint | Cost |
    |---|---|
    | `DELETE /comments/:id` | Free |
    | `GET /ideas/:id/votes` | Free |
    | `DELETE /ideas/:id/vote` | Free |
    | `DELETE /bookmarks/:id` | Free |
    | `GET /categories` | Free |
    | `GET /stats` | Free |
    | `GET /account/credits` | Free |
    | `GET /account/profile` | Free |
    | `GET /account/usage` | Free |
  </Accordion>
</AccordionGroup>

---

## Free Credit

<Tip>
Every new developer gets **$5.00 free credit** when generating their first API key. No credit card required.
</Tip>

That's enough for:
- **500** list or detail requests (`GET /ideas`, `GET /ideas/:slug`)
- **250** search requests (`GET /ideas/search`)
- **500** comment reads or vote actions
- **Unlimited** free endpoints (categories, stats, account)

---

## Adding Credits

Top up your balance from the [Developer Dashboard](https://openprojectidea.com/developer-api) with any amount between **$5 and $500**. Payments are processed securely via Dodo Payments and credits are added instantly.

## Checking Your Balance

Use the free `/account/credits` endpoint:

```bash
curl -H "x-api-key: YOUR_API_KEY" \
  https://openprojectidea.com/v1/api/account/credits
```

```json Response
{
  "data": {
    "balance": 4.50,
    "balance_cents": 450,
    "lifetime_purchased": 10.00,
    "lifetime_used": 0.50,
    "currency": "USD"
  }
}
```

---

## Insufficient Credits

If your balance is too low, you'll receive a `402` response:

```json 402 - Insufficient Credits
{
  "error": "insufficient_credits",
  "message": "Not enough API credits. Balance: $0.03, cost: $0.02",
  "balance_cents": 3,
  "cost_cents": 2
}
```

<Warning>
Credits are non-refundable. Monitor your balance using the `/account/credits` endpoint to avoid interruptions.
</Warning>
